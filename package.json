{
  "name": "form-builder",
  "version": "1.0.0",
  "description": "Form Builder Application",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && node verify-build.js",
    "start": "npm run start --prefix backend",
    "verify": "node verify-build.js",
    "postbuild": "echo 'Build process completed'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "cloudinary": "^1.41.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}