{
  "name": "form-builder",
  "version": "1.0.0",
  "description": "Form Builder Application",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "build": "npm ci --prefix backend --only=production && npm ci --prefix frontend && cd frontend && npm run build && cd .. && node verify-build.js",
    "build:dev": "npm install --prefix backend && npm install --prefix frontend && cd frontend && npm run build && cd .. && node verify-build.js",
    "start": "npm run start --prefix backend",
    "verify": "node verify-build.js",
    "postbuild": "echo 'Build process completed'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}